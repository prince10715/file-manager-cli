# File Manager CLI

A simple command-line file manager built using Node.js.  
It allows basic file system operations directly from the terminal.

---

## 🚀 Features

- List files and directories
- Create files and folders
- Delete files and folders
- Read file contents
- Move/rename files
- Simple CLI interface

---

## 📦 Installation

Clone the repository:

```bash
git clone https://github.com/prince10715/file-manager-cli.git
